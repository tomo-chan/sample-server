{
  "name": "sample-server",
  "version": "1.0.0",
  "description": "Sample app -- server side",
  "main": "server.js",
  "scripts": {
    "test": "istanbul cover node_modules/mocha/bin/_mocha --print none --report html -- tests/*.js -R tap",
    "testdebug": "mocha --debug --debug-brk ./tests/*.js",
    "start": "node server.js",
    "debug": "node-debug server.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.14.0",
    "fs": "0.0.1-security",
    "multer": "^1.2.0",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^3.2.0",
    "supertest": "^2.0.1"
  }
}
